# Landing 3D model

Place your Blender export here so the landing page uses it:

- **Filename:** `d1bfff0f-a4bb-4fa1-981e-9812a4c2380a.glb`
- **Format:** GLB (recommended) or GLTF

The app will load this asset automatically. If the file is missing, the default pill capsule is shown instead.
