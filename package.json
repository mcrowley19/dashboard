{
  "private": true,
  "description": "Metricare monorepo root — run frontend and backend from their own folders.",
  "scripts": {
    "dev": "cd backend && python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:backend": "cd backend && python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:alt": "cd backend && python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8001"
  }
}
